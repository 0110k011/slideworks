import { makeStyles } from '@material-ui/core/styles';

const useStyles = makeStyles({
  root: {
    maxWidth: 345,
  },
  media: {
    height: 240,
    borderRadius: 6,
  },
  description: {
    height: 120,
  },
  imgContainer: {
    padding: 15,
  },
});

export default useStyles;
